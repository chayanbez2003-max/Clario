/**
 * Parser Service — Step 5 (NLP Classification)
 *
 * This module will contain the NLP pipeline for extracting
 * job application data from raw email content.
 *
 * Planned exports:
 *   - parseEmail(emailObject)      → { company, role, status, confidence }
 *   - classifyStatus(emailText)    → 'applied' | 'interview' | 'offer' | 'rejected'
 *   - extractCompanyName(emailText) → string
 *
 * Input: email objects from gmailReaderService.fetchEmailBody()
 * Output: structured application data consumed by applicationController
 *
 * NOT IMPLEMENTED — placeholder for Milestone 5
 */

module.exports = {}
